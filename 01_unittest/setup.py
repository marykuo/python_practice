from setuptools import setup, find_packages

setup(
    name="01_unittest",
    version="0.1",
    packages=find_packages(),
    install_requires=[],
    setup_requires=[],
    tests_require=[],
    entry_points={
        "console_scripts": [],
    },
)
